trigger:
- main

pool:
    vmImage: ubuntu-latest

variables:
    buildConfiguration: 'Releae'

steps:
-   script: dotnet build --configuration $(buildConfiguration)
    displayName: 'dotnet build $(buildConfiguraiton)'